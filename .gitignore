__pycache__
*.pyc
*.swp
/dist
.DS_Store

# Local dev venv used by build/windows/buildExe-onlykey.ps1
venv/

# Runtime data dirs created by the app
.buskill/
build/.buskill/

# PyInstaller intermediate/output dirs and logs (release artifacts go to
# GitHub Releases, not the repo)
build/windows/build/
build/windows/dist/
build/windows/build.log

# Buildozer / python-for-android intermediate and output dirs
# (.buildozer/ holds the downloaded Android SDK+NDK, ~3GB)
android/.buildozer/
android/bin/
